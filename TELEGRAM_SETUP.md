# Настройка Telegram уведомлений

## Шаг 1: Создание бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Уведомления Социальный ремонт")
   - Введите username бота (должен заканчиваться на `bot`, например: `socremont_bot`)
4. **Сохраните токен бота** - он будет выглядеть примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

## Шаг 2: Получение Chat ID

### Вариант 1: Через @userinfobot
1. Найдите бота **@userinfobot** в Telegram
2. Отправьте ему любое сообщение
3. Он вернет ваш Chat ID (число, например: `123456789`)

### Вариант 2: Через группу (если хотите отправлять в группу)
1. Создайте группу в Telegram
2. Добавьте вашего бота в группу
3. Найдите бота **@RawDataBot** и добавьте его в группу
4. Отправьте любое сообщение в группу
5. @RawDataBot покажет Chat ID группы (будет отрицательным числом, например: `-1001234567890`)

## Шаг 3: Настройка config.js

1. Откройте файл `config.js`
2. Замените `YOUR_BOT_TOKEN_HERE` на токен бота из шага 1
3. Замените `YOUR_CHAT_ID_HERE` на Chat ID из шага 2

Пример:
```javascript
const telegramConfig = {
    botToken: '123456789:ABCdefGHIjklMNOpqrsTUVwxyz',
    chatId: '123456789'
};
```

## Шаг 4: Проверка работы

1. Откройте любую страницу сайта
2. Заполните форму (имя и телефон)
3. Отправьте форму
4. Проверьте Telegram - должно прийти уведомление в формате:

```
🔔 Новая заявка с социальный-ремонт.рф (Ульяновск)

Страница: Ремонт стиральной машины

👤 Имя: Иван Иванов

📞 Телефон: +7 (999) 123-45-67

📋 Тип заявки: Основная форма

⏰ Время: 22.12.2025, 21:07:10
```

## Формат уведомлений

Уведомления отправляются в следующем формате:
- **Город** определяется автоматически из URL или localStorage
- **Страница** определяется автоматически по имени файла
- **Тип заявки** определяется по классу формы:
  - `hero__form` → "Основная форма"
  - `discount-form__form` или `contact-form__form` → "Форма скидки 15%"
  - Форма в модальном окне → "Форма обратного звонка"

## Безопасность

⚠️ **ВАЖНО**: 
- Не публикуйте `config.js` в публичных репозиториях
- Не делитесь токеном бота и Chat ID
- Если токен был скомпрометирован, создайте нового бота через @BotFather

## Решение проблем

### Уведомления не приходят
1. Проверьте, что токен бота правильный
2. Проверьте, что Chat ID правильный
3. Откройте консоль браузера (F12) и проверьте ошибки
4. Убедитесь, что бот не заблокирован

### Ошибка "Telegram конфигурация не найдена"
- Убедитесь, что `config.js` подключен перед `script.js` в HTML
- Проверьте, что файл `config.js` существует и содержит правильные данные

### Ошибка CORS
- Telegram API не требует CORS, но если возникают проблемы, убедитесь, что сайт открывается через HTTP/HTTPS, а не file://

